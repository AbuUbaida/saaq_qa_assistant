"""Data ingestion utilities (collect -> clean/chunk -> embed -> index).

This package holds reusable functions. The `scripts/` folder calls into these
functions, and backend code can import from here directly.
"""


